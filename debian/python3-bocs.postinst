#!/bin/sh

set -e


ROOT_DIR="/home/pi/bocs"
LOG_DIR="${ROOT_DIR}/logs"
DIR="${LOG_DIR}/SENSOR_ARRAY_A"
USER="pi"
mkdir -p ${DIR}

#--------------------------------------
# TODO Remove when move to single array
DIR2="${LOG_DIR}/SENSOR_ARRAY_B"
mkdir -p ${DIR2}
#--------------------------------------

chown -R ${USER}:${USER} ${ROOT_DIR}

